import type { ProviderModel, ProviderHealthData } from "../types";

function generateHistory(points: number, base: number, variance: number): number[] {
  return Array.from({ length: points }, (_, i) =>
    Math.round((base + Math.sin(i * 0.4) * variance * 0.5 + (Math.random() - 0.5) * variance * 0.6) * 10) / 10,
  );
}

export const mockProviders: ProviderModel[] = [
  {
    id: "openai",
    name: "OpenAI",
    icon: "brain",
    status: "healthy",
    requestsPerMin: 2850,
    avgLatency: 340,
    throughput: 1250,
    fallbackRate: 0.5,
    tokensPerSecond: 4800,
    uptime: 99.97,
    errorRate: 0.12,
    history: generateHistory(24, 340, 45),
    region: "us-east-1",
    modelCount: 12,
    lastIncident: null,
  },
  {
    id: "claude",
    name: "Claude",
    icon: "sparkles",
    status: "healthy",
    requestsPerMin: 1820,
    avgLatency: 420,
    throughput: 980,
    fallbackRate: 1.2,
    tokensPerSecond: 3600,
    uptime: 99.94,
    errorRate: 0.08,
    history: generateHistory(24, 420, 55),
    region: "us-west-2",
    modelCount: 6,
    lastIncident: null,
  },
  {
    id: "gemini",
    name: "Gemini",
    icon: "sparkle",
    status: "degraded",
    requestsPerMin: 980,
    avgLatency: 680,
    throughput: 720,
    fallbackRate: 4.8,
    tokensPerSecond: 2100,
    uptime: 98.72,
    errorRate: 0.45,
    history: generateHistory(24, 680, 110),
    region: "us-central1",
    modelCount: 8,
    lastIncident: "2026-05-29T14:30:00Z",
  },
  {
    id: "ollama",
    name: "Ollama",
    icon: "container",
    status: "healthy",
    requestsPerMin: 3200,
    avgLatency: 180,
    throughput: 2100,
    fallbackRate: 0.3,
    tokensPerSecond: 8900,
    uptime: 99.99,
    errorRate: 0.03,
    history: generateHistory(24, 180, 20),
    region: "on-prem",
    modelCount: 24,
    lastIncident: null,
  },
  {
    id: "deepseek",
    name: "DeepSeek",
    icon: "search",
    status: "warning",
    requestsPerMin: 750,
    avgLatency: 520,
    throughput: 640,
    fallbackRate: 3.1,
    tokensPerSecond: 2800,
    uptime: 99.45,
    errorRate: 0.28,
    history: generateHistory(24, 520, 65),
    region: "ap-east-1",
    modelCount: 4,
    lastIncident: "2026-05-29T10:15:00Z",
  },
];

export const mockProviderHealthData: ProviderHealthData = {
  providers: mockProviders,
  summary: {
    totalProviders: 5,
    healthyCount: 3,
    degradedCount: 1,
    downCount: 0,
    totalRequests: 9600,
    avgLatency: 428,
    totalTokensPerSecond: 22200,
    overallUptime: 99.61,
  },
};
